import { TraceEvent } from '../../enums/traceEvent';
/**
 * Logger interface to capture log events
 */
export interface ILogger {
    trace(event: TraceEvent, properties?: {
        [key: string]: string;
    }, appName?: string): void;
    traceError(event: TraceEvent, error: any, appName?: string): void;
}
